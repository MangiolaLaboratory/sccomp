% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{calculate_na_fraction_contribution}
\alias{calculate_na_fraction_contribution}
\title{Calculate fraction contribution for NA values in design matrix}
\usage{
calculate_na_fraction_contribution(
  data_with_na,
  formula,
  factor_names,
  continuous_names,
  interaction_names
)
}
\arguments{
\item{data_with_na}{A data frame containing the data with NA values}

\item{formula}{A formula object specifying the model}

\item{factor_names}{A character vector of factor variable names}

\item{continuous_names}{A character vector of continuous variable names}

\item{interaction_names}{A character vector of interaction term names}
}
\value{
A list containing:
\itemize{
\item factor_contributions: A list of factor level contributions
\item continuous_contributions: A list of continuous variable contributions
\item interaction_contributions: A list of interaction term contributions
}
}
\description{
This function calculates the fraction contribution for NA values in a design matrix.
It handles both factor and continuous variables, and their interactions.
For factors, it calculates the fraction of each level's contribution.
For continuous variables, it uses the mean value.
For interactions, it multiplies the fractions of the interacting variables.
}
\examples{
# Example usage:
data <- data.frame(
  group = factor(c("A", "B", NA)),
  type = factor(c("X", NA, "Y")),
  cont = c(1, 2, NA)
)
formula <- ~ group * type + cont
factor_names <- c("group", "type")
continuous_names <- "cont"
interaction_names <- "group:type"
calculate_na_fraction_contribution(data, formula, factor_names, continuous_names, interaction_names)

}
